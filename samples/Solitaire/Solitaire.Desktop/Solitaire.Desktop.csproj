<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RollForward>Major</RollForward>
    <PublishReadyToRun>false</PublishReadyToRun>
    <TieredCompilation>false</TieredCompilation>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Solitaire</RootNamespace>
  </PropertyGroup>

  <!-- Apos.Shapes is intentionally NOT pinned here. Its version flows transitively from
       the engine; its shader is embedded in the assembly, so it needs no content-pipeline
       wiring. -->
  <ItemGroup>
    <PackageReference Include="MonoGame.Framework.DesktopGL" />
    <PackageReference Include="MonoGame.Content.Builder.Task" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Solitaire.Common\Solitaire.Common.csproj" />
  </ItemGroup>

  <!--
    Link Common's Gum project files into Desktop's Content/ output.
    See ../Solitaire.GumPackage.targets for the UseGumPackage toggle.
  -->
  <Import Project="..\Solitaire.GumPackage.targets" />

  <ItemGroup Condition="'$(UseGumPackage)' != 'true'">
    <Content Include="..\Solitaire.Common\Content\GumProject\**\*.*"
             Exclude="..\Solitaire.Common\Content\GumProject\**\*.gumpkg"
             Link="Content\GumProject\%(RecursiveDir)%(Filename)%(Extension)"
             CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <ItemGroup Condition="'$(UseGumPackage)' == 'true'">
    <Content Include="..\Solitaire.Common\Content\GumProject\GumProject.gumpkg"
             Link="Content\GumProject\GumProject.gumpkg"
             CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>
</Project>
